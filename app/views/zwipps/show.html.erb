<ul class="list-group">
  <%= render "shared/zwipp" %>
</ul>

<%- if user_signed_in? && current_user == @user %>
  <%= button_to 'delete zwipp', zwipp_path(@zwipp), method: :delete, class: 'btn btn-default' %>
<%- end %>
